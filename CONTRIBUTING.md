# Contributing

A useful contribution should improve a real recurring task: add a failure case, sharpen triggering, improve a decision rule, reduce generic advice, add a deterministic helper, or contribute an external-use example.

Before a pull request:

```bash
pip install pyyaml
python scripts/validate_skills.py
```

Please describe the real task, the failure you observed, and how the change improves repeatability.
