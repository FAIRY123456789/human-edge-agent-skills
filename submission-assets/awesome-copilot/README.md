# Awesome Copilot Intake Drafts

The first two candidates are `skill-from-scars` and `voice-to-work`. Their copyable intake fields are recorded in `docs/GITHUB_AWESOME_COPILOT_SUBMISSION_FINAL.md`.

No machine-ready intake JSON is emitted yet: the project explicitly retired `v0.3.0` as a future marketplace review artifact, and `v0.4.0` is gated on both Codex and Cursor runtime validation. Cursor is unavailable on this host, so the immutable `ref` and full `sha` fields must remain blocked instead of being fabricated or pointed back to `v0.3.0`.

Do not edit `github/awesome-copilot`'s `plugins/external.json` directly. A future submission must use the official external-plugin issue form and requires explicit user approval.
